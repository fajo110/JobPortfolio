Class Name: ManageDirectory
Subclasses: MoveDirectory

Responsiblities:
-> mv - from one location to another, a directory gets moved
-> After directory gets relocated, it rename's the directory
-> Either relative or absolute paths, can be the path for the files
-> For an existing file destination, a backup can get created
-> cp - without being removed from it's original destination, cp can copy 
the files and directories.

Collaboration:
Cat